# Ablation study

The manifest defines A severity, B ATDI, C Stage 1, D numeric pointwise, E transformer, F LambdaMART without transformer, G with transformer, H with transformer+LLM, and I full diversity. Each uses the same fixed group manifests and reports global and slice metrics with group-bootstrap intervals. Expert/historical primary results and weak-label auxiliary results must be separate. No ablation results are fabricated before execution.

