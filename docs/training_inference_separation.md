# Training and inference separation

After metrics, deltas, resolution, and overall improvement are label-generation fields only. Current inference may use current Arcan data, static rules, trained outputs, and training-only priors. Historical frequency is fitted after splitting. This prevents target and project leakage.
