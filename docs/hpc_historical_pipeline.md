# HPC historical pipeline

Set DSARP data/output/repository/tool/model-cache variables, replace every `REPLACE_*` placeholder, then run `sbatch hpc/submit_historical_training_pipeline.sh`. `afterok` orders select → mine → Arcan → datasets → transformer → ranker → evaluation. No university partition, account, module, GPU, or email is assumed.
