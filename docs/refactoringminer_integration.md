# RefactoringMiner integration

Command, version, timeout, retries, and output are configured in `configs/refactoring_miner.yaml`. Parsed operations retain left/right locations, paths, elements, and normalized IDs. Element, component, package, source, target, prefix, and Jaccard evidence controls smell association. Tests use a mock; real mining belongs on HPC.
