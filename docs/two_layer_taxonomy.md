# Two-layer taxonomy

Layer 1 is concrete RefactoringMiner operations. Layer 2 is user-facing architectural strategies. `configs/architectural_mapping.yaml` maps operations plus metric evidence to strategies with confidence. Reports call Layer 2 “inferred,” never “detected.”
