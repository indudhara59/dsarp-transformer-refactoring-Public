# Smell matching

Because vertex IDs may change, matching uses normalized smell type, central component, affected components/elements, path, and repository. Type must match. Weighted exact/fuzzy scoring yields persisted, resolved, and newly introduced transitions. Close competing matches remain ambiguous rather than being forced.
