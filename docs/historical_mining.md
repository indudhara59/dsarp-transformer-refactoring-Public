# Historical mining

Selection supports keywords, time bounds, Java filtering, non-merge selection, limits, known commits, and seeded sampling. Keywords nominate candidates but do not prove improvement. Git worktrees preserve the caller's checkout. Each commit directory contains metadata, RefactoringMiner JSON, and three before/after Arcan exports. Expensive stages are cached and failures are isolated per commit.
