# Arcan before/after

Parent and current commits are analyzed separately. Both load through the existing Stage 1 discovery, schema mapping, filter, and join. The runner validates all three exports and records command output and version. Using Arcan in training and inference avoids incompatible smell definitions.
