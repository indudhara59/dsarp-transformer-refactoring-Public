# Repository inference

Repository mode resolves a local revision, creates a detached worktree, invokes Arcan once, validates the three exports, and calls the shared CSV pipeline. It is an adapter, not another recommender. Without source context, the report explicitly says it uses architecture-level Arcan metrics only.
